/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['links.papareact.com', 'img.icons8.com', 'heroicons.com', 'images.unsplash.com']
  }
}

module.exports = nextConfig
